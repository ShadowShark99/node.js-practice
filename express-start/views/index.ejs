<!-- index.ejs -->

<html>
  <head>
    <title>Homepage</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <%- include('navbar', {links: links}) %>
    <ul>
      <% users.forEach((user, i) => { %> <%- include('user', {user: user, i:
      i}); %> <% }); %>
    </ul>
  </body>
</html>
