<% if(locals.users) {  %>
  <ul>
    <% for(let i = 0; i < users.length; i++) { %>
      <li> <%= users[i].id %>: <%= users[i].username %></li>
    <% } %>
  </ul>
<% } else { %>
  <h1>No Users Found</h1>
<% } %>
<form action="/" method="get">
  <button type="submit">Back Home</button>
</form>